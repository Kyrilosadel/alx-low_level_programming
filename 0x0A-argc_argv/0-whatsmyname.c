#include <stdio.h>

/**
 * main - Entoint
 * @argc: The number of commguments
 * @argv: An array containing the-line arguments
 *
 * Return: 0 on sess
 */
int main(int argc, char *argv[])
{
(void)argc;
printf("%s\n", argv[0]);
return (0);
}

